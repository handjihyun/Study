# ============================================
library(shiny)

# UI 요소 구성 부분 --------------------------
ui <- fluidPage(
  
)

# SERVER 구현 부분 ---------------------------
server <- function(input, output, session){
  
}

# Shiny App 실행 부분 ------------------------
shinyApp(ui, server)